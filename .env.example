# All API keys are server-side only (not exposed to client)

# Last.fm - Shows "Now Playing" in widgets
LASTFM_API_KEY=your_lastfm_api_key
LASTFM_USERNAME=your_lastfm_username

# Strava - Shows best recent run with race equivalency
# Get credentials from https://www.strava.com/settings/api
STRAVA_CLIENT_ID=your_client_id
STRAVA_CLIENT_SECRET=your_client_secret
STRAVA_REFRESH_TOKEN=your_initial_refresh_token

# Upstash Redis - Stores rotating Strava tokens
# Get from https://console.upstash.com/ (free tier available)
UPSTASH_REDIS_REST_URL=your_upstash_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_token
